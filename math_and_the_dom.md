<script>
    // Primitives
    // Math
    1 + 2 * 5 / 3
    5 + (3 * 10)
    // Strings
    "Hello" + "World"
    // Booleans
    true == false

    // Collections
    // Variables
    var a = "Howdy"
    var b = " Folks"
    a + b
    var a = 3
    a + b
    var b = 2
    a + b
    // Arrays
    var kids = ["Nevin", "Ada"]
    var parents = ["Cory", "Liz"]
    kids + parents
    kids.concat(parents)
    console.log(kids)
    console.log(parents)
    var family = kids.concat(parents)

    // Hashes or Dictionaries
    var addr = {
        "key": "value",
        "address": "4336 Stanton Ave",
        "City": "Pittsburgh"
    }
    console.log(addr)
    console.log(addr["address"])

    // DOM
    // JQuery Setup
    // Selectors
    var element = $("Some element")
    // element is an array

</script>